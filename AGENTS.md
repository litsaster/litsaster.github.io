Đã ghi nhớ. Từ giờ:

- **Desktop (có hover)**: tooltip hiện khi rê chuột, popup hiện khi click
- **Mobile (không hover)**: không có tooltip, chỉ có popup khi click
- **Git push**: Chỉ push khi được yêu cầu rõ ràng "push lên git"
